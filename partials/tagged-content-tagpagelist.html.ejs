
<ul class='list-group'>
<%
    for (var j = 0; j < entries.length; j++) {
        var entry = entries[j];
        %>
        <li class="list-group-item">
            <a href="/<%= entry.renderpath %>"><%= entry.metadata.title %></a> 
            <% if (entry.metadata.teaser) { %><%= entry.metadata.teaser %><% } %>
        </li><%
    }
%>
</ul>