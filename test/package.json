{
  "name": "akashacms-theme-bootstrap-test",
  "version": "1.0.0",
  "description": "Test suite for akashacms-theme-bootstrap module",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "test": "mocha ./index",
    "setup": "npm install && cd .. && npm install && npm install ../akasharender --no-save",
    "clean": "rm -rf node_modules out"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^5.1.x",
    "mocha": "^10.8.x",
    "akashacms-base": "../../akashacms-base"
  }
}
